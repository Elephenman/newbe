# 🧬 fastq-subset-sampler

**从FASTQ文件中随机采样指定数量或比例的reads**

## 使用方法

```bash
cd fastq-subset-sampler
python fastq-subset-sampler.py
```

运行后按提示依次输入参数，所有参数均有默认值，直接回车即可使用默认值。

## 依赖

无外部依赖，纯Python标准库

## 输出

脚本运行后会在当前目录生成结果文件，具体文件名见运行提示。

## License

MIT